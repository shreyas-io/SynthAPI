services:
  web-apps:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: mock-stack-web-apps
    command: >
      sh -lc "
        CI=true pnpm install --frozen-lockfile &&
        pnpm --filter @mock-stack/api run migrate:latest &&
        pnpm --filter @mock-stack/api run seed:local &&
        pnpm --filter @mock-stack/mockapi-engine run migrate:latest &&
        pnpm dev:local
      "
    depends_on:
      postgres-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      PORT: "${API_PORT}"
      HOST: "0.0.0.0"
      CORS_WHITELISTED_DOMAINS: "${CORS_WHITELISTED_DOMAINS}"
      LOCAL_TEST_USER_USERNAME: "${LOCAL_TEST_USER_USERNAME:-test_user}"
      LOCAL_TEST_USER_PASSWORD: "${LOCAL_TEST_USER_PASSWORD:-password}"
      VITE_API_BASE_URL: "${VITE_API_BASE_URL}"
      APPLICATION_DB_USER: "${POSTGRES_USER}"
      APPLICATION_DB_PASS: "${POSTGRES_PASSWORD}"
      APPLICATION_DB_HOST: postgres
      APPLICATION_DB_PORT: "5432"
      APPLICATION_DB_NAME: "${APPLICATION_DB_NAME}"
      DB_USER: "${POSTGRES_USER}"
      DB_PASS: "${POSTGRES_PASSWORD}"
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: "${APPLICATION_DB_NAME}"
      API_GATEWAY_DB_USER: "${POSTGRES_USER}"
      API_GATEWAY_DB_PASS: "${POSTGRES_PASSWORD}"
      API_GATEWAY_DB_HOST: postgres
      API_GATEWAY_DB_PORT: "5432"
      API_GATEWAY_DB_NAME: "${API_GATEWAY_DB_NAME}"
      AGENT_ORCHESTRATION_DB_USER: "${POSTGRES_USER}"
      AGENT_ORCHESTRATION_DB_PASS: "${POSTGRES_PASSWORD}"
      AGENT_ORCHESTRATION_DB_HOST: postgres
      AGENT_ORCHESTRATION_DB_PORT: "5432"
      AGENT_ORCHESTRATION_DB_NAME: "${AGENT_ORCHESTRATION_DB_NAME}"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASS: "${REDIS_PASSWORD}"
      CLOUDFLARE_ACCOUNT_ID: "${CLOUDFLARE_ACCOUNT_ID}"
      CLOUDFLARE_AI_GATEWAY_ID: "${CLOUDFLARE_AI_GATEWAY_ID}"
      CLOUDFLARE_AI_GATEWAY_TOKEN: "${CLOUDFLARE_AI_GATEWAY_TOKEN}"
      OPENROUTER_API_KEY: "${OPENROUTER_API_KEY}"
      OLLAMA_BASE_URL: "${OLLAMA_BASE_URL}"
    expose:
      - "3001"
    ports:
      - "5173:5173"
      - "9229:9229"
    volumes:
      - .:/repo
      - node-modules:/repo/web-apps/node_modules
      - api-node-modules:/repo/web-apps/apps/api/node_modules
      - web-node-modules:/repo/web-apps/apps/web/node_modules
      - mockapi-engine-node-modules:/repo/web-apps/packages/mockapi-engine/node_modules
      - agent-orchestration-engine-node-modules:/repo/web-apps/packages/agent-orchestration-engine/node_modules

  gateway:
    image: kong:3.9
    container_name: mock-stack-gateway
    depends_on:
      - web-apps
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /kong/declarative/kong.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: "off"
    ports:
      - "8787:8000"
    volumes:
      - ./gateway/kong.yml:/kong/declarative/kong.yml:ro

  postgres:
    image: postgres:17-alpine
    container_name: mock-stack-postgres
    environment:
      POSTGRES_DB: "${APPLICATION_DB_NAME}"
      POSTGRES_USER: "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      APPLICATION_DB_NAME: "${APPLICATION_DB_NAME}"
      API_GATEWAY_DB_NAME: "${API_GATEWAY_DB_NAME}"
      AGENT_ORCHESTRATION_DB_NAME: "${AGENT_ORCHESTRATION_DB_NAME}"
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init-databases.sh:/docker-entrypoint-initdb.d/001-init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d postgres"]
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-init:
    image: postgres:17-alpine
    container_name: mock-stack-postgres-init
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: "${POSTGRES_USER}"
      PGPASSWORD: "${POSTGRES_PASSWORD}"
      APPLICATION_DB_NAME: "${APPLICATION_DB_NAME}"
      API_GATEWAY_DB_NAME: "${API_GATEWAY_DB_NAME}"
      AGENT_ORCHESTRATION_DB_NAME: "${AGENT_ORCHESTRATION_DB_NAME}"
    volumes:
      - ./docker/postgres/init-databases.sh:/init-databases.sh:ro
    command: ["sh", "/init-databases.sh"]

  redis:
    image: redis:7.4-alpine
    container_name: mock-stack-redis
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: mock-stack-redis-commander
    depends_on:
      redis:
        condition: service_healthy
    environment:
      REDIS_HOSTS: "local:redis:6379:0:${REDIS_PASSWORD}"
    ports:
      - "8081:8081"

volumes:
  node-modules:
  api-node-modules:
  web-node-modules:
  mockapi-engine-node-modules:
  agent-orchestration-engine-node-modules:
  postgres-data:
  redis-data:
